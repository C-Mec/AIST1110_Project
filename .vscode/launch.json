{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run main.py (.conda)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}\\main.py",
      "console": "integratedTerminal",
      "env": {
        "PATH": "${env:PATH};${workspaceFolder}\\.conda\\Library\\bin;${workspaceFolder}\\.conda\\DLLs"
      }
    }
  ]
}
